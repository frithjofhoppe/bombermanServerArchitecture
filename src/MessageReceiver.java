import game.Game;
import messages.DropBomb;
import messages.Message;
import messages.PlayerMoved;

public class MessageReceiver {
    Session session;

    void performMessage(Message message) {
    }

    void dropBomb(DropBomb dropBomb) {
    }

    void moveGameClient(PlayerMoved playerMoved) {
    }
}
